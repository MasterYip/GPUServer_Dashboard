# Test training run on 4090-5
name: test-train-4090-5
project: pegasusmodye
description: "Single-GPU test training run on 4090-5"

defaults:
  work_dir: /data/masteryip/PegasusMoDye/PDPlanner
  python: /data/conda/envs/pegasusmodye/bin/python
  cfg: g1prdp_cond_diffuse.yaml
  log_dir: /tmp/gpu-jobs
  env:
    OMNI_KIT_ACCEPT_EULA: "YES"
    PYTHONPATH: /data/masteryip/PegasusMoDye/PDPlanner
  overrides:
    # Use absolute zarr path — avoids broken symlinks on non-primary servers
    dataset.zarr_path: /data/masteryip/PegasusMoDye/RLTracker/artifacts/normal_loco/motion_dyed_text_t0.01.zarr
    training.num_epochs: 5
    training.tqdm_interval_sec: 30
    logging.mode: offline

  path_remap:
    - match: "4090-"
      exclude: ["4090-3"]
      map:
        "/data/masteryip/": "/mnt/4090-3/masteryip/"
        "/data/conda/": "/mnt/4090-3/conda/"

command: |
  cd {work_dir}
  {python} train.py --cfg {cfg} --exp_name {exp_name} {overrides} >> {log_file} 2>&1

tasks:
  - name: test-4090-5
    overrides: {}
